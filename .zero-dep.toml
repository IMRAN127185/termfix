[submission]
name = "TermFix"
track = "A"
target_runtime = "Python 3.14.7"
pitch = "Proof-backed, source-aware terminal corrections that never guess, edit, or execute silently."

[dependencies]
runtime = []
