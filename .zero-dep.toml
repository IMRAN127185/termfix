[submission]
name = "TermFix"
track = "A"
target_runtime = "Python 3.14.7"
pitch = "A portable, source-aware terminal that proves corrections and never executes them silently."

[dependencies]
runtime = []
